# 研发接口规范

## 用户查询接口

- 接口路径：`GET /api/users/{id}`
- 功能：根据用户 ID 查询用户资料
- 返回字段：`id`、`name`、`email`、`department`

## 错误码规范

- `400`：请求参数错误
- `401`：未认证
- `403`：无权限
- `404`：资源不存在
- `500`：系统内部错误

## 开发约定

- 所有接口返回统一 JSON 结构：`code`、`data`、`msg`
- 新增接口前需要补充接口文档
- 重要接口变更必须同步测试同学
